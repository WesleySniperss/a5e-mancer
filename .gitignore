.claude/
*.local.json
node_modules/

# Build artifact (attached to GitHub Releases, not committed)
module.zip
# Crash dumps / junk
*.stackdump

# The illustrated layout works over artwork the repository does not own.
assets/sheet/
