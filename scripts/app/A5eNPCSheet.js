/* The NPC sheet is defined in A5eCharacterSheet.js, beside the class it
   extends — see the note there. This file stays so that module.json, and any
   manifest a running world still holds in memory, keep resolving. */
export { A5eNPCSheet } from './A5eCharacterSheet.js';
