/**
 * Content imported from a5e.tools, as a5e item documents, for the world's
 * "A5e Mancer: Imported" compendium (see ImportedPack).
 *
 * One file per import written by hand, each exporting its documents with fixed
 * ids; this list and the converted content in generated.js are what the pack
 * is built from. Content converted by tools/import goes to generated.js and its
 * JSON, not here.
 */
import { DREAD_KNIGHT } from './dreadKnight.js';

export const IMPORTED = [
  ...DREAD_KNIGHT
];
